<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.gov.justica.cidadaoalerta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light">
        <activity android:name=".TransparenciaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConveniosActivity" />
        <activity android:name=".AjustesActivity" />
        <activity android:name=".convenios.BuscarActivity" />
        <activity android:name=".convenios.DetalhesActivity" />
        <activity android:name=".convenios.Empenho" />
        <activity android:name=".convenios.ExecucaoFinanceiraActivity"></activity>
    </application>

</manifest>
